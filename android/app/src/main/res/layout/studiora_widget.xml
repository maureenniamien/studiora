<?xml version="1.0" encoding="utf-8"?>
<!--
  MISE À JOUR (2026-09-17) : l'arbre et la mascotte séparés sont remplacés
  par une unique image de scène (studiora_widget_scene_*) qui contient déjà
  le décor complet (mascotte + arbre/pousse) pour l'état du jour. L'état
  affiché est décidé côté natif (StudioraWidgetProvider.sceneDrawableFor),
  à partir de ce que la WebView a poussé via AndroidWidgetSync.setState(...).
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/widget_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/studiora_widget_bg"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:padding="12dp">

    <ImageView
        android:id="@+id/widget_scene"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="1"
        android:scaleType="centerInside"
        android:contentDescription="Studiora"
        android:src="@drawable/studiora_widget_scene_todo" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="2"
        android:orientation="vertical"
        android:paddingStart="10dp">

        <TextView
            android:id="@+id/widget_label"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Studiora"
            android:textColor="#166534"
            android:textStyle="bold"
            android:textSize="12sp" />

        <TextView
            android:id="@+id/widget_tip"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="2dp"
            android:text="Chargement..."
            android:textColor="#3A3A3A"
            android:textSize="13sp"
            android:maxLines="4"
            android:ellipsize="end" />

    </LinearLayout>

</LinearLayout>

